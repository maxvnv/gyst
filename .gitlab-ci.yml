image: "ringcentral/maven"

stages:
  - build
  - deploy

build:
  script:
    - mvn clean package
  artifacts:
    paths:
      - target/
  stage: build


deploy:
  script:
    - ls -l
    - docker image build -t gyst .
    - docker image ls
  stage: deploy
